{
  "kind": "collectionType",
  "collectionName": "questions",
  "info": {
    "singularName": "question",
    "pluralName": "questions",
    "displayName": "Question",
    "description": "One MCQ question. `correctOptionId` is the answer key and must never be sent to a student."
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "prompt": {
      "type": "text",
      "required": true
    },
    "options": {
      "type": "json",
      "required": true
    },
    "correctOptionId": {
      "type": "string",
      "required": true,
      "private": true
    },
    "order": {
      "type": "integer",
      "default": 0,
      "required": true
    },
    "quiz": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::quiz.quiz",
      "inversedBy": "questions"
    }
  }
}
