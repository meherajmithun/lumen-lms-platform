import{getApprovedStories}from'@/lib/api/stories';export const metadata={title:'Stories | Lumen'};export default async function Page(){const stories=await getApprovedStories().catch(()=>[]);return <main className="mx-auto max-w-6xl px-5 py-16"><p className="text-xs font-semibold uppercase tracking-widest text-pine">Student voices</p><h1 className="mt-3 text-4xl font-semibold">Stories from Lumen learners</h1><div className="mt-10 grid gap-5 md:grid-cols-2">{stories.map(s=><article key={s.documentId} className="rounded-xl border bg-card p-6"><h2 className="text-xl font-semibold">{s.title}</h2><p className="mt-1 text-sm text-pine">{s.studentName}</p><p className="mt-5 whitespace-pre-wrap text-sm leading-relaxed text-muted-foreground">{s.body}</p></article>)}{!stories.length&&<p className="text-sm text-muted-foreground">No approved stories yet.</p>}</div></main>}
