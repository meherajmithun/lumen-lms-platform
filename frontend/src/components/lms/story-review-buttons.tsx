'use client';import{useTransition}from'react';import{useRouter}from'next/navigation';import{toast}from'sonner';import{Button}from'@/components/ui/button';import{reviewStoryAction}from'@/app/actions/stories';
export function StoryReviewButtons({id}:{id:string}){const[pending,start]=useTransition();const router=useRouter();function act(decision:'approved'|'rejected'){start(async()=>{const r=await reviewStoryAction(id,decision);if(r.ok){toast.success(`Story ${decision}`);router.refresh()}else toast.error(r.error)})}return <div className="flex gap-2"><Button size="sm" disabled={pending} onClick={()=>act('approved')}>Approve</Button><Button size="sm" variant="destructive" disabled={pending} onClick={()=>act('rejected')}>Reject</Button></div>}
