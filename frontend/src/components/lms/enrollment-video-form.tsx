'use client';import{useState,useTransition}from'react';import{toast}from'sonner';import{Input}from'@/components/ui/input';import{Button}from'@/components/ui/button';import{Label}from'@/components/ui/label';import{saveEnrollmentVideoAction}from'@/app/actions/admin';
export function EnrollmentVideoForm({initialUrl=''}:{initialUrl?:string}){const[url,setUrl]=useState(initialUrl);const[pending,start]=useTransition();return <form className="mb-6 rounded-xl border bg-card p-5" onSubmit={e=>{e.preventDefault();start(async()=>{const r=await saveEnrollmentVideoAction(url);if(r.ok)toast.success('Enrollment video updated');else toast.error(r.error)})}}><Label htmlFor="videoUrl">How to enroll video URL</Label><div className="mt-2 flex gap-2"><Input id="videoUrl" type="url" value={url} onChange={e=>setUrl(e.target.value)} placeholder="https://youtube.com/embed/..." required/><Button type="submit" disabled={pending}>{pending?'Saving…':'Save video'}</Button></div><p className="mt-2 text-xs text-muted-foreground">Use an embeddable video URL. It will appear in the student enrollment guide.</p></form>}
