'use client';import{useState,useTransition}from'react';import{toast}from'sonner';import{Button}from'@/components/ui/button';import{Input}from'@/components/ui/input';import{Label}from'@/components/ui/label';import{Textarea}from'@/components/ui/textarea';import{submitStoryAction}from'@/app/actions/stories';
export function StoryForm(){const[pending,start]=useTransition();const[sent,setSent]=useState(false);if(sent)return <div role="status" className="rounded-xl border border-pine/30 bg-pine-wash p-8 text-center"><h2 className="font-semibold">Story submitted successfully</h2><p className="mt-2 text-sm text-muted-foreground">A Content Manager will review your story before it is published.</p></div>;return <form className="max-w-2xl space-y-5" onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget);start(async()=>{const r=await submitStoryAction({title:String(f.get('title')),body:String(f.get('body'))});if(r.ok){toast.success('Story submitted successfully');setSent(true)}else toast.error(r.error)})}}><div className="space-y-2"><Label htmlFor="title">Story title</Label><Input id="title" name="title" required minLength={3} maxLength={140}/></div><div className="space-y-2"><Label htmlFor="body">Your story</Label><Textarea id="body" name="body" required rows={12} maxLength={4000} placeholder="Share what you learned and how Lumen helped you…"/></div><Button type="submit" disabled={pending}>{pending?'Submitting…':'Submit story'}</Button></form>}
